@model   List<MvcProjectArch.UI.Models.Catolog.ProductGroupTreeNode> 
@{
    ViewBag.Title = "TraverByType";
    Layout = "~/Views/Shared/MainMaster.cshtml";
}

<section class="content-header">
    <h1>
        @ViewBag.type sıralama

        <small>Ürün</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i>Ürün</a></li>
        <li class="active">Ürün</li>
    </ol>
</section>
<!-- Main content -->
<section class="content">


        @foreach (var item in Model)
        {
            if(item.ProductBase.Products.Count>0)
            { 
            <table>
                <tr>
                    <th>Ürün Adı</th>
                    <th>Marka Adı</th>
                    <th>Model Adı</th>
                    <th>Maliyet</th>
                    <th>Fiyat</th>
                    <th></th>
                </tr>
                @foreach (var item2 in item.ProductBase.Products)
                {



              
                    <tr>
                        <td>@item2.ProductName</td>
                        <td>@item2.BrandName</td>
                        <td>@item2.ModelName</td>
                        <td>@item2.ProductCost</td>
                        <td>@item2.ProductPrice</td>
                        <td><a href="/Product/CheckoutAdd/@item2.ProductId">Sepete Ekle</a></td>

                    </tr>
                }
            </table>
        }

        }
    

        }
    </ul>
</section>

